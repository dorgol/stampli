# put your real key in .env (NOT in source control)
OPENAI_API_KEY=sk-xxx

# default data locations (override as needed)
META_PATH=data/embeddings_out/reviews_with_embeddings.parquet
CHROMA_PATH=data/index/chroma_db
COLLECTION=reviews
